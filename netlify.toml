[build]
  command = "npm --prefix frontend ci && npm --prefix frontend run build"
  publish = "frontend/dist"

[build.environment]
  VITE_STUDIO_ORIGIN = "https://studio.identifyng.com"

# Single-page app routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

